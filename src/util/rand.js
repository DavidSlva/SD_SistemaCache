function generarNumeroAleatorio(m) {
    return Math.floor(Math.random() * (m + 1));
}
/**
 * 
 * @param {Number} to 
 * @returns - RAnd number 1 - to
 */
function generarValorUnoA(to) {
    return Math.floor(Math.random() * to) + 1;
}